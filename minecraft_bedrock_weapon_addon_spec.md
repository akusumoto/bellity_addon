# Minecraft Bedrock Edition 武器アドオン仕様書

> 2026-09-20 update: the Chotto Netherite Axe recipe is now specified in [AXE_RECIPE_SPEC.md](AXE_RECIPE_SPEC.md). Any earlier "recipe undecided" text for this axe is superseded by that specification.

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | Minecraft Bedrock Edition 武器アドオン仕様書 |
| 状態 | 3武器の実装済み・実機検証待ち |
| 対象 | Minecraft Bedrock Edition |
| 対象バージョン | Bedrock Edition 1.21.110以降（対象端末での実機確認待ち） |
| 実装担当 | Codex |
| 画像・モデル制作 | Blockbench |

> **実装時確認:** Minecraft本体の正確なバージョン番号を確認し、`format_version`、`min_engine_version`、コンポーネント、Script APIを、その時点のMicrosoft公式安定版仕様に合わせる。

## 2. 目的

Minecraft Bedrock Editionへ、剣、斧、投擲武器など複数種類の独自武器を継続的に追加できるアドオンを構築する。最初に剣を実装して共通基盤を確立し、その後の武器追加で同じ命名規則、フォルダ構成、品質基準を再利用する。

## 3. 基本方針

- Behavior PackとResource Packを1組の武器アドオンとして管理する。
- 武器ごとに定義ファイルを分割し、追加・変更・削除の影響範囲を限定する。
- 共通の名前空間、命名規則、言語ファイル、テクスチャ登録方式を使用する。
- Java Edition用Modではなく、Bedrock Edition用アドオンとして実装する。
- 実装時点の公式安定版仕様を利用し、廃止済みの記述を避ける。
- 実験的機能やScript APIは、対象武器の機能に必要な場合だけ採用する。
- Windows版Minecraft Bedrock Editionでの開発とテストを基本とする。
- 最終成果物は `.mcaddon` と、編集可能なソース一式とする。

### 3.1 Windows版Bedrockの開発用保存先

このPCのWindows版Minecraft Bedrockでは、次のフォルダを使用する。実際のフォルダ名は `Users\Shared` である。

```text
%appdata%\Minecraft Bedrock\Users\Shared\games\com.mojang
```

エクスプローラーで開く手順：

1. `Win + R`を押して「ファイル名を指定して実行」を開く。
2. 次のパスを貼り付ける。

   ```text
   %appdata%\Minecraft Bedrock\Users\Shared\games\com.mojang
   ```

3. 「OK」を押す。

開発中のBehavior PackとResource Packは、それぞれ次のフォルダへ保存する。

| 種類 | 保存先 |
|---|---|
| Behavior Pack | `%appdata%\Minecraft Bedrock\Users\Shared\games\com.mojang\development_behavior_packs\Bellity_BP` |
| Resource Pack | `%appdata%\Minecraft Bedrock\Users\Shared\games\com.mojang\development_resource_packs\Bellity_RP` |

本アドオンの配置例：

```text
%appdata%\Minecraft Bedrock\Users\Shared\games\com.mojang\
├─ development_behavior_packs\
│  └─ Bellity_BP\
│     ├─ manifest.json
│     ├─ items\
│     ├─ recipes\
│     └─ scripts\
└─ development_resource_packs\
   └─ Bellity_RP\
      ├─ manifest.json
      ├─ textures\
      └─ texts\
```

Codexは開発中、上記2フォルダを直接作成・更新する。配布時は両方をまとめて `.mcaddon` を生成する。

通常導入されたパック用として、同じ `com.mojang` 配下に `behavior_packs` と `resource_packs` も存在するが、開発作業には `development_behavior_packs` と `development_resource_packs` を使用する。

コンテンツエラーのログは次の場所へ出力される。

```text
%appdata%\Minecraft Bedrock\logs\
```

ログを利用するには、Minecraftの設定にある「Creator」タブでコンテンツログを有効にする。

Minecraft Previewを使用する場合は、通常版と別の次の場所を使用する。

```text
%appdata%\Minecraft Bedrock Preview\Users\Shared\games\com.mojang
```

旧UWP版では次の場所が使われていたが、最新版の正式な開発先としては使用しない。

```text
%localappdata%\Packages\Microsoft.MinecraftUWP_8wekyb3d8bbwe\LocalState\games\com.mojang
```

> **実装時確認:** 対象PCでGDK側の `com.mojang` フォルダが存在することを確認する。Minecraftを一度起動しても存在しない場合に限り、インストール中のエディションと旧UWPフォルダの有無を調査する。

## 4. 開発段階

| 段階 | 内容 | 状態 |
|---|---|---|
| 第1段階 | 2Dテクスチャの剣を1種類追加し、共通基盤を確立 | 実装済み・実機検証待ち |
| 第2段階 | 斧を1種類以上追加 | 実装済み・実機検証待ち |
| 第3段階 | 投擲武器を1種類以上追加 | 実装済み・実機検証待ち |
| 第4段階 | 特殊能力、演出、バランス調整 | 基本能力を実装済み・調整は要検討 |
| 第5段階 | 必要に応じて3Dモデル化 | 要検討 |

## 5. 武器の分類

### 5.1 近接武器

- 剣
- 斧
- 将来候補：槍、短剣、ハンマーなど

共通候補項目：

- 攻撃力
- 耐久値
- エンチャント可否
- 修理素材
- 攻撃時の耐久消費
- クラフトレシピ
- 特殊効果

> **要検討:** Bedrock Editionのアイテムコンポーネントで斧固有の挙動をどこまで再現するか。剣と異なる攻撃性能、ブロック破壊適性、盾への作用などは、現行仕様と実現可能性を確認して決定する。

### 5.2 投擲武器

候補：

- 手裏剣
- 投げナイフ
- 槍・ジャベリン
- 爆発物
- 魔法弾を発射する武器

投擲武器で検討する項目：

- 投擲後に消費するか、回収できるか
- スタック可能数
- 発射速度とクールダウン
- 飛翔速度、重力、射程
- 命中ダメージ
- ノックバック
- 命中時の特殊効果
- 地面や壁へ刺さる表現
- 投擲時・命中時の音
- パーティクル
- プレイヤー以外が使用できるか

> **要検討:** 投擲武器は、既存の投射物の仕組み、カスタムエンティティ、Script APIのいずれで実装するかを武器ごとに判断する。安定版コンポーネントのみで要件を満たせない場合は、実験的機能の使用可否をユーザーと協議する。

## 6. 武器共通仕様

各武器は次の情報を持つ。

| 項目 | 内容 |
|---|---|
| 内部識別子 | `<namespace>:<weapon_id>` |
| 日本語表示名 | `ja_JP.lang`に定義 |
| 英語表示名 | `en_US.lang`に定義 |
| 武器分類 | sword、axe、throwableなどプロジェクト内分類を付与 |
| テクスチャ | 武器ごとの透過PNG |
| 入手方法 | クラフト、クリエイティブ、コマンドなど |
| 性能 | 武器分類に応じた個別設定 |
| 説明 | 必要に応じてREADMEや仕様一覧へ記載 |

### 6.1 武器管理表

武器を追加するたびに次の表へ追記する。

| ID | 日本語名 | 分類 | 攻撃力 | 耐久値／個数 | 入手方法 | 特殊能力 | 状態 |
|---|---|---|---:|---:|---|---|---|
| `bellity:bellity_sword` | ベリティソード | 剣 | 32 | 1233 | クラフト | 叩くとランダムにアイテムがドロップする | 実装済み・実機検証待ち |
| `bellity:noboru_netherite_axe` | ちょっとネザライトの斧 | 斧 | 6 | 1200 | クリエイティブ・コマンド（レシピ未定） | 特になし | 実装済み・実機検証待ち |
| `bellity:sun_bigman_light` | 太陽の巨人の光 | 投擲武器 | 10 | 500 | クラフト | 投げたものが当たると燃える | 実装済み・実機検証待ち |

### 6.2 第1段階の剣：ベリティソード

| 項目 | 仕様 |
|---|---|
| 分類 | 剣 |
| アイテムID | `bellity:bellity_sword` |
| 日本語名 | ベリティソード |
| 英語名 | Bellity Sword |
| 攻撃力 | 32 |
| 耐久値 | 1233 |
| 能力 | 叩くとランダムにアイテムがドロップする |
| 画像ファイル | `model_data/bellity_sword.png` |

クラフトレシピ（上段から順に記載。`-` は空欄）：

```text
H G -
- C -
- B -
```

| 記号 | 素材 |
|---|---|
| H | 火打ち石の打ち金 |
| G | 金インゴット |
| C | クリーパーの顔 |
| B | 棒 |

実装上、剣の有効な命中ごとに、リンゴ・石炭・鉄インゴット・金インゴット・レッドストーン・ラピスラズリ・エメラルド・ダイヤモンドから等確率で1個をドロップする。攻撃力は基本攻撃力1にアイテムの追加ダメージ31を加えた値。候補と確率はバランス調整時に変更可能とする。修理素材、エンチャント可否、レアリティは未定。

### 6.3 2つ目の武器：ちょっとネザライトの斧

| 項目 | 仕様 |
|---|---|
| 分類 | 斧 |
| アイテムID | `bellity:noboru_netherite_axe` |
| 日本語名 | ちょっとネザライトの斧 |
| 英語名 | Chotto Netherite Axe |
| 攻撃力 | 6 |
| 耐久値 | 1200 |
| クラフトレシピ | 未定 |
| 能力 | 特になし |
| 画像ファイル | `model_data/noboru_netherite_axe.png` |

木材タグを持つブロックへの採掘速度倍率を6とする。攻撃力は基本攻撃力1にアイテムの追加ダメージ5を加えた値。クラフトレシピは未定で、現時点ではクリエイティブまたはコマンドで取得する。

### 6.4 3つ目の武器：太陽の巨人の光

| 項目 | 仕様 |
|---|---|
| 分類 | 投擲武器 |
| アイテムID | `bellity:sun_bigman_light` |
| 日本語名 | 太陽の巨人の光 |
| 英語名 | Sun Bigman Light |
| 攻撃力 | 10 |
| 耐久値 | 500 |
| 能力 | 投げたものが当たると燃える |
| 画像ファイル | `model_data/sun_bigman_light.png` |

クラフトレシピ（`-` は空欄。`Tr` は1マスの素材）：

| 段 | 左 | 中央 | 右 |
|---|---|---|---|
| 上段 | T | - | - |
| 中段 | - | A | - |
| 下段 | - | - | Tr |

| 記号 | 素材 |
|---|---|
| T | 松明 |
| A | 矢 |
| Tr | トライデント |

実装上、使用時に耐久値を1消費し、命中したエンティティに10ダメージを与えて5秒間燃やす。弾は標準の雪玉エンティティを利用し、投げたアイテム本体は消費しない。投擲間隔は最低7ゲームティック。投擲物の外観は雪玉のままで、専用表示は今後の検討事項とする。

## 7. テクスチャ・モデル仕様

### 7.1 2Dテクスチャ

| 項目 | 仕様 |
|---|---|
| 制作ツール | Blockbench |
| 形式 | 透過PNG |
| 基準サイズ | 16×16 pxまたは32×32 px |
| 描画 | アンチエイリアスを使わないピクセルアート |
| ファイル名 | 英小文字・数字・アンダースコアのみ |
| 配置先 | Resource Packの `textures/items/` 配下 |

武器間で解像度、輪郭、光源方向、色数などのデザインルールをできるだけ統一する。

> **要検討:** アドオン全体で16×16と32×32のどちらを標準にするか決定する。武器ごとに混在させる場合は見た目の一貫性を確認する。

### 7.2 3Dモデル

初期版では対象外とする。将来採用する場合は、モデル、テクスチャ、アタッチャブル、持ち方、三人称表示、一人称表示を武器ごとに確認する。

## 8. ソース上のパック構成案

```text
weapon_addon/
├─ behavior_pack/
│  ├─ manifest.json
│  ├─ items/
│  │  ├─ swords/
│  │  ├─ axes/
│  │  └─ throwables/
│  ├─ recipes/
│  │  ├─ swords/
│  │  ├─ axes/
│  │  └─ throwables/
│  ├─ entities/                 # 必要な場合のみ
│  ├─ scripts/                  # Script API採用時のみ
│  └─ pack_icon.png
├─ resource_pack/
│  ├─ manifest.json
│  ├─ textures/
│  │  ├─ item_texture.json
│  │  ├─ items/
│  │  │  ├─ swords/
│  │  │  ├─ axes/
│  │  │  └─ throwables/
│  │  └─ entity/                # 投射物等で必要な場合のみ
│  ├─ models/                   # 3Dモデル採用時のみ
│  ├─ attachables/              # 必要な場合のみ
│  ├─ entity/                   # カスタムエンティティ採用時のみ
│  ├─ render_controllers/       # 必要な場合のみ
│  ├─ texts/
│  │  ├─ languages.json
│  │  ├─ en_US.lang
│  │  └─ ja_JP.lang
│  └─ pack_icon.png
├─ tests/
│  └─ test_checklist.md
└─ README.md
```

このソース構成から、`behavior_pack`の内容をWindows側の`development_behavior_packs\Bellity_BP`へ、`resource_pack`の内容を`development_resource_packs\Bellity_RP`へ配置する。Codexを対象PC上で直接実行する場合は、最初から開発用保存先を作業対象にしてもよい。

> **実装時確認:** Bedrock Editionが対象フォルダのサブディレクトリを期待どおり読み込むことを実機確認する。問題があるファイル種別は標準フォルダ直下へ配置する。

## 9. 識別子と命名規則

- アドオン全体で1つの固有名前空間を使用する。
- 名前空間に `minecraft` は使用しない。
- 識別子とファイル名には、英小文字、数字、アンダースコアを使用する。
- ファイル名に空白、日本語、大文字を使用しない。
- 武器分類が分かるIDを推奨する。

例：

```text
akira:netherite_sword
akira:netherite_axe
akira:throwing_knife
```

> **要検討:** 正式な名前空間を決定する。また、バニラのネザライト武器と混同する名称を使用する場合は、表示名と内部IDの区別を明確にする。

## 10. クラフト・入手仕様

- 近接武器は既存武器に近い形状のレシピを基本候補とする。
- 投擲武器は複数個をまとめて作れるレシピも検討する。
- 強力な武器は希少素材、鍛冶台、段階的強化などで入手難度を調整する。
- テスト中はコマンドおよびクリエイティブから取得できるようにする。

> **要検討:** 武器ごとに、素材、作成数、作業台・鍛冶台の要否、クラフト可否、修理方法を決定する。

## 11. バランス方針

- バニラの剣、斧、弓、クロスボウ、トライデントなどを比較基準とする。
- 攻撃力だけでなく、射程、速度、耐久値、消費数、入手難度、特殊能力を含めて調整する。
- ネザライト装備を超える性能には、相応の入手コストまたは制約を設定する。
- 対人戦と対モブ戦で極端な性能差が出ないか確認する。

> **要検討:** 主な利用環境がソロ、家族・友人とのマルチプレイ、一般配布のどれかを決定する。利用環境によりバランス基準を変更する。

## 12. Script APIと実験的機能

### 12.1 採用基準

- アイテムコンポーネントだけで実現できる機能は、Script APIを使用しない。
- 複雑な命中処理、回収処理、クールダウン制御などで必要な場合に検討する。
- Script APIを採用する場合も、実装時点の安定版モジュールを優先する。
- 実験的機能が必要な場合、導入上の制約と将来の互換性リスクを明記する。

### 12.2 将来の共通処理候補

- 武器IDと性能値の一元管理
- 投擲時と命中時の共通処理
- クールダウン管理
- 特殊効果の付与
- デバッグログ

## 13. 動作確認

### 13.1 全武器共通

- Behavior PackとResource Packが認識される。
- ワールド読み込み時にコンテンツログのエラーがない。
- クリエイティブインベントリまたはコマンドで取得できる。
- 日本語名と英語名が正しく表示される。
- インベントリ、一人称、三人称で表示が破綻しない。
- 設定した性能と耐久・消費処理が反映される。
- セーブ後およびワールド再読み込み後も保持される。
- マルチプレイで各参加者から正しく見える。
- `.mcaddon` を正常にインポートできる。

### 13.2 近接武器

- 想定した攻撃ダメージになる。
- 攻撃時に耐久値が減少する。
- 修理とエンチャントが仕様どおり動作する。
- 対象ブロックへの破壊適性を設定した場合、正しく動作する。

### 13.3 投擲武器

- 使用時に正しい投射物が生成される。
- 飛翔速度、重力、射程が仕様どおりである。
- エンティティとブロックへの命中を正しく判定する。
- ダメージ、ノックバック、特殊効果が仕様どおりである。
- 消費・回収・クールダウンが仕様どおりである。
- 複数プレイヤーが同時に使用しても誤作動しない。

## 14. 武器追加手順

1. 武器管理表へ予定項目を追加する。
2. 表示名、ID、分類、性能、入手方法を決定する。
3. Blockbenchでテクスチャまたはモデルを作成する。
4. Behavior Packへアイテム定義を追加する。
5. Resource Packへ画像、テクスチャ登録、翻訳を追加する。
6. 必要に応じてレシピ、エンティティ、スクリプトを追加する。
7. 共通・分類別テストを実施する。
8. 武器管理表とREADMEを更新する。
9. `.mcaddon` を再生成してインポート確認する。

## 15. 成果物

- 編集可能なBehavior Pack一式
- 編集可能なResource Pack一式
- 必要に応じたScript APIコード
- インポート用 `.mcaddon`
- 各武器のPNG、モデルなどの制作素材
- 導入・開発・武器追加手順を記載したREADME
- 武器一覧とテストチェックリスト

## 16. 実装前に決定する共通事項

1. アドオン名：Bellity Weapons
2. 名前空間：`bellity`
3. 標準テクスチャサイズ
4. 対象Minecraftバージョン
5. 実験的機能：現行実装では使用しない
6. Script API：剣のドロップと投擲処理に使用する（`@minecraft/server` 2.0.0）
7. 主な利用環境と配布範囲
8. 武器全体の強さの基準
9. パックアイコン

## 17. 第1段階の剣について決定する事項

ベリティソードの実装内容は6.2節に記載済み。追加実装時に次を決定する。

1. 修理素材
2. エンチャント可否
3. レアリティ

## 18. 実機検証の残件

- 対象端末のMinecraftバージョンを確認する。
- テスト用ワールドに `.mcaddon` をインポートし、Behavior PackとResource Packを有効にする。
- 13節の共通・分類別動作チェックを行い、Content Logにエラーがないことを確認する。

以後の武器は「武器追加手順」に従い、同じアドオンへ継続追加する。
